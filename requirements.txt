# ─────────────────────────────────────────
# 단가표 마진 분석기 — 의존성 목록
# 설치: pip install -r requirements.txt
# ─────────────────────────────────────────

# 웹 UI 프레임워크
streamlit>=1.35.0

# OCR 엔진 (이미지/PDF에서 텍스트 추출)
easyocr>=1.7.1

# PDF -> 이미지 변환 (OCR 전처리)
PyMuPDF>=1.24.0

# LLM API 클라이언트
openai>=1.35.0

# 데이터 처리 및 엑셀 출력
pandas>=2.2.0
openpyxl>=3.1.2
xlrd>=2.0.1

# 이미지 처리 (EasyOCR 내부 의존)
Pillow>=10.3.0
opencv-python-headless>=4.9.0

# 수치 계산 (EasyOCR 내부 의존)
numpy>=1.26.0
torch>=2.2.0

# ─────────────────────────────────────────
# EXE 빌드 시 torch CPU 전용 버전 사용 권장
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# (GPU 버전 대비 용량 3GB -> 600MB 절감)
# ─────────────────────────────────────────
